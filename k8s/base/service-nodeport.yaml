apiVersion: v1
kind: Service
metadata:
  name: oauth2-server-nodeport
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    app: oauth2-server
