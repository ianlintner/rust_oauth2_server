name: Semgrep

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "27 4 * * 5"

permissions:
  contents: read

jobs:
  semgrep:
    permissions:
      contents: read
      security-events: write
      actions: read
    name: Scan
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@v7.0.1 # v6

      - name: Run Semgrep scan
        run: semgrep scan --config auto --sarif --output=semgrep.sarif
        env:
          # Optional: set SEMGREP_APP_TOKEN in repository secrets to
          # enable Semgrep Cloud Platform features (managed policies,
          # findings triage, etc.). Basic scanning works without it.
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          sarif_file: semgrep.sarif
        if: success() || failure()
